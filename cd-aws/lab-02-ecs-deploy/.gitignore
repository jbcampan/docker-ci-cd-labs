# task-definition.json is generated by `terraform apply` — do not commit it
# manually. Commit the file only after Terraform has filled in the real ARNs.
# If you want to track it: remove this line and run `git add task-definition.json`.
# task-definition.json

# Environment secrets
.env

# Terraform working files
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/*.tfvars
!terraform/terraform.tfvars.example
