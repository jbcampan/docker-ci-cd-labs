# Copy this file to .env and adjust values as needed.
# docker-compose.yml reads these variables at startup.

# Port exposed on the host for the Flask API
API_PORT=5000

# Port exposed on the host for Redis (optional — only needed for local inspection)
REDIS_PORT=6379
